# دليل استخدام Node.js App في Hostinger

## ⚠️ مهم جداً:

**Node.js غير متاح في terminal على Hostinger.** هذا طبيعي!

**يجب استخدام Node.js App من لوحة التحكم فقط.**

---

## ✅ الخطوات الصحيحة:

### 1. اذهب إلى لوحة تحكم Hostinger

### 2. ابحث عن "Node.js App" أو "Node.js"

عادة يكون في:
- **Advanced** → **Node.js App**
- أو **Websites** → **Node.js App**
- أو **Applications** → **Node.js**

### 3. تحقق من الإعدادات:

#### أ. إنشاء Node.js App جديد (إذا لم يكن موجوداً):

1. اضغط **"Create Node.js App"** أو **"Add Node.js App"**
2. اختر النطاق: `gotawfeer.com`
3. اختر الإطار: **Express** (أو Vue.js أو Vite)

#### ب. إعدادات Node.js App:

- **Root Directory:** اتركه **فارغاً** (لأن الملفات في `public_html`)
- **Start Command:** `node server/index.mjs`
- **Node Version:** اختر **18.x** أو **20.x**
- **Port:** اتركه على الإعداد الافتراضي (عادة 3000)

#### ج. متغيرات البيئة (Environment Variables):

أضف:
```
NODE_ENV=production
```

### 4. حفظ وتشغيل:

1. اضغط **"Save"** أو **"Create"**
2. اضغط **"Start"** أو **"Restart"**
3. انتظر 30-60 ثانية
4. تحقق من **Status** → يجب أن يكون **"Running"**

### 5. تحقق من Logs:

1. اذهب إلى **"Logs"** في Node.js App
2. يجب أن ترى رسائل مثل:
   ```
   Nitro server started
   Server listening on port 3000
   ```
   أو
   ```
   Nuxt server ready
   ```

---

## 🔍 إذا لم تجد Node.js App:

### الخيار 1: البحث في لوحة التحكم

ابحث عن:
- "Node.js"
- "Node"
- "Application"
- "App"

### الخيار 2: تحقق من نوع الاستضافة

Node.js App متاح فقط في:
- ✅ **VPS**
- ✅ **Cloud Hosting**
- ✅ **Business Hosting**

إذا كان لديك **Shared Hosting**، قد تحتاج إلى ترقية.

### الخيار 3: اتصل بدعم Hostinger

اسألهم:
- "كيف أفعّل Node.js App؟"
- "هل Node.js متاح في خطتي؟"

---

## 📝 قائمة التحقق:

- [ ] وجدت Node.js App في لوحة التحكم
- [ ] أنشأت Node.js App جديد (إذا لزم الأمر)
- [ ] Root Directory: **فارغ**
- [ ] Start Command: `node server/index.mjs`
- [ ] Node Version: 18.x أو 20.x
- [ ] Status: **Running**
- [ ] تحققت من Logs - لا توجد أخطاء
- [ ] الموقع يعمل الآن (لا يوجد 403)

---

## 🆘 إذا استمرت المشكلة:

### 1. تحقق من Logs:

في Node.js App → Logs، ابحث عن:
- أخطاء في Start Command
- أخطاء في الملفات
- أخطاء في الأذونات

### 2. تحقق من الملفات:

تأكد من:
- ✅ `server/index.mjs` موجود
- ✅ `public/` موجود
- ✅ الأذونات صحيحة

### 3. جرب حذف `.htaccess`:

احذف `htaccess.` مؤقتاً من `public_html`

### 4. اتصل بدعم Hostinger:

أخبرهم:
- أنك تحصل على 403 Forbidden
- أن Node.js App Status = Running
- أرسل لهم Logs من Node.js App

---

## 💡 ملاحظة مهمة:

**لا تحاول تشغيل Node.js من terminal.** 

Hostinger يستخدم Node.js App لإدارة Node.js تلقائياً. هذا هو الطريقة الصحيحة.

