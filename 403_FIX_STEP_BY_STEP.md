# حل مشكلة 403 Forbidden - خطوة بخطوة

## ⚠️ المشكلة:
خطأ 403 Forbidden يعني أن الخادم يرفض الوصول. هذا **ليس** مشكلة في الكود، بل في إعدادات Hostinger.

---

## ✅ الحلول بالترتيب:

### الحل 1: تحقق من Node.js App (الأهم!)

في لوحة تحكم Hostinger → **Node.js App**:

1. **تأكد من أن Node.js App مفعل:**
   - Status يجب أن يكون **"Running"** أو **"Active"**
   - إذا كان **"Stopped"** → اضغط **"Start"** أو **"Restart"**

2. **تحقق من Start Command:**
   ```
   node dist/server/index.mjs
   ```
   أو إذا كان Root Directory = `dist`:
   ```
   node server/index.mjs
   ```

3. **تحقق من Root Directory:**
   - إذا رفعت `dist` في الجذر → اتركه **فارغاً**
   - إذا رفعت `dist` في مجلد فرعي → ضع المسار (مثلاً: `dist`)

4. **تحقق من Node Version:**
   - يجب أن يكون **18.x** أو **20.x**

5. **تحقق من السجلات (Logs):**
   - اذهب إلى **Logs** في Node.js App
   - ابحث عن أي أخطاء
   - إذا وجدت أخطاء، أرسلها

---

### الحل 2: حذف `.htaccess` مؤقتاً

ملف `.htaccess` قد يسبب مشاكل مع Node.js App.

**على السيرفر:**
```bash
# احذف أو أعد تسمية .htaccess
rm .htaccess
# أو
mv .htaccess .htaccess.backup
```

ثم جرب الوصول إلى الموقع مرة أخرى.

---

### الحل 3: تحقق من الأذونات

**على السيرفر (عبر SSH أو File Manager):**
```bash
chmod 755 dist
chmod 755 dist/server
chmod 644 dist/server/index.mjs
chmod -R 755 dist/public
```

---

### الحل 4: تحقق من الملفات

تأكد من أن:
- ✅ `dist/server/index.mjs` موجود
- ✅ `dist/public` موجود ويحتوي على الملفات
- ✅ جميع الملفات تم رفعها بشكل صحيح

**للتحقق:**
```bash
ls -la dist/server/index.mjs
ls -la dist/public
```

---

### الحل 5: جرب إعادة تشغيل Node.js App

في لوحة تحكم Hostinger:
1. اذهب إلى **Node.js App**
2. اضغط **"Restart"** أو **"Stop"** ثم **"Start"**
3. انتظر 30-60 ثانية
4. جرب الوصول إلى الموقع

---

### الحل 6: تحقق من Port Configuration

في بعض الحالات، قد تحتاج إلى:
- **Port:** اتركه على الإعداد الافتراضي (عادة 3000 أو 8080)
- **Domain:** تأكد من أن النطاق مرتبط بشكل صحيح

---

## 🔍 التشخيص:

### إذا كان Node.js App يعمل:
- تحقق من السجلات (Logs) لمعرفة الأخطاء
- تحقق من Start Command
- جرب حذف `.htaccess`

### إذا كان Node.js App لا يعمل:
- تحقق من Start Command
- تحقق من أن `dist/server/index.mjs` موجود
- تحقق من Node Version
- تحقق من الأذونات

---

## 📝 قائمة التحقق السريعة:

- [ ] Node.js App **مفعل** و **يعمل**
- [ ] Start Command صحيح: `node dist/server/index.mjs`
- [ ] `dist/server/index.mjs` موجود
- [ ] `dist/public` موجود
- [ ] الأذونات صحيحة (755 للمجلدات، 644 للملفات)
- [ ] `.htaccess` محذوف أو معطل
- [ ] Node Version: 18.x أو 20.x
- [ ] تم إعادة تشغيل Node.js App بعد التغييرات

---

## 🆘 إذا استمرت المشكلة:

1. **تحقق من السجلات (Logs)** في Hostinger
2. **اتصل بدعم Hostinger** وأخبرهم:
   - أنك تستخدم Node.js App
   - Start Command: `node dist/server/index.mjs`
   - أنك تحصل على 403 Forbidden
   - أرسل لهم السجلات (Logs)

---

## 💡 ملاحظة مهمة:

إذا كان Hostinger يستخدم **Node.js App** بشكل صحيح، **لا تحتاج إلى `.htaccess`**. Node.js server سيتولى معالجة جميع الطلبات.

`.htaccess` يستخدم فقط مع Apache/PHP، وليس مع Node.js.

