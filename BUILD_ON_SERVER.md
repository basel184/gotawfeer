# البناء على السيرفر (Hostinger) - الطريقة الموصى بها

## ✅ المفهوم:
- رفع **الكود المصدري** (source code) إلى Hostinger
- Hostinger يقوم بالبناء تلقائياً
- لا حاجة للبناء محلياً

---

## 📤 الملفات التي يجب رفعها إلى Hostinger:

ارفع **جميع ملفات المشروع** إلى Hostinger (إلى `public_html` أو المجلد المخصص):

### ✅ ارفع:
- ✅ `package.json`
- ✅ `nuxt.config.ts`
- ✅ `tsconfig.json`
- ✅ `pages/` (جميع الملفات)
- ✅ `components/` (جميع الملفات)
- ✅ `composables/` (جميع الملفات)
- ✅ `server/` (جميع الملفات)
- ✅ `public/` (جميع الملفات)
- ✅ `assets/` (جميع الملفات)
- ✅ `plugins/` (جميع الملفات)
- ✅ `middleware/` (جميع الملفات)
- ✅ `layouts/` (جميع الملفات)
- ✅ `.npmrc` (إن وجد)
- ✅ أي ملفات إعدادات أخرى

### ❌ لا ترفع:
- ❌ `node_modules/` (سيتم تثبيتها على السيرفر)
- ❌ `.nuxt/` (سيتم إنشاؤه عند البناء)
- ❌ `.output/` (سيتم إنشاؤه عند البناء)
- ❌ `dist/` (سيتم إنشاؤه عند البناء)
- ❌ `.git/`
- ❌ `.env` (استخدم Environment Variables في Hostinger)

---

## ⚙️ إعدادات Hostinger:

### 1. في لوحة تحكم Hostinger → Node.js App:

#### Build Settings:
- **Build Command:** `npm run build`
- **Output Directory:** `.output` أو اتركه فارغاً

#### App Settings:
- **Root Directory:** اتركه **فارغاً** (إذا رفعت الملفات في الجذر)
- **Start Command:** `node .output/server/index.mjs`
- **Node Version:** **18.x** أو **20.x**

#### Environment Variables:
```
NODE_ENV=production
```

---

## 🔄 العملية:

### 1. رفع الملفات:
ارفع جميع ملفات المشروع (ما عدا `node_modules`, `.nuxt`, `.output`)

### 2. Hostinger يقوم تلقائياً بـ:
1. تثبيت الحزم: `npm install`
2. البناء: `npm run build`
3. تشغيل التطبيق: `node .output/server/index.mjs`

### 3. بعد كل تحديث:
1. ارفع الملفات المحدثة
2. Hostinger سيقوم بالبناء تلقائياً
3. التطبيق سيعمل مباشرة

---

## 📁 البنية النهائية على Hostinger:

```
public_html/
├── package.json
├── nuxt.config.ts
├── tsconfig.json
├── pages/
├── components/
├── composables/
├── server/
├── public/
├── assets/
├── plugins/
├── middleware/
├── layouts/
├── .npmrc
└── .output/          ← يتم إنشاؤه تلقائياً بعد البناء
    ├── server/
    │   └── index.mjs
    └── public/
```

---

## ✅ المميزات:

1. **أسهل:** لا حاجة للبناء محلياً
2. **تلقائي:** Hostinger يقوم بكل شيء
3. **أحدث:** دائماً أحدث نسخة من الكود
4. **أسرع:** لا حاجة لرفع ملفات كبيرة

---

## 🔧 حل المشاكل:

### إذا فشل البناء:
1. تحقق من **Logs** في Hostinger
2. تحقق من أن جميع الملفات تم رفعها
3. تحقق من `package.json` و `nuxt.config.ts`

### إذا ظهر 403 Forbidden:
1. تحقق من **Start Command:** `node .output/server/index.mjs`
2. تحقق من أن Node.js App **مفعل**
3. تحقق من **Logs** لمعرفة الأخطاء

---

## 📝 ملاحظات:

- تأكد من رفع **جميع** الملفات المطلوبة
- لا ترفع `node_modules` - سيتم تثبيتها تلقائياً
- استخدم **Environment Variables** في Hostinger بدلاً من `.env`

