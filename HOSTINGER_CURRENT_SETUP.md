# إعدادات Hostinger الحالية

## 📁 البنية الحالية على السيرفر:

من الصورة، يبدو أن الملفات موجودة في `public_html` مباشرة:

```
public_html/
├── builds./
├── public/          ← الملفات الثابتة
├── server/          ← كود الخادم
├── htaccess.       ← ملف .htaccess
└── nitro.json
```

## ⚠️ المشكلة:

الملفات موجودة **مباشرة** في `public_html` وليس في مجلد `dist`.

## ✅ الحل:

### في لوحة تحكم Hostinger → Node.js App:

**Start Command يجب أن يكون:**
```
node server/index.mjs
```

**وليس:**
```
node dist/server/index.mjs
```

### إعدادات Node.js App:

- **Root Directory:** اتركه **فارغاً** (لأن الملفات في الجذر)
- **Start Command:** `node server/index.mjs`
- **Node Version:** 18.x أو 20.x

---

## 🔧 خطوات الإصلاح:

### 1. تحقق من Start Command:
في Hostinger → Node.js App → Start Command:
```
node server/index.mjs
```

### 2. تحقق من أن Node.js App مفعل:
- Status يجب أن يكون **"Running"**

### 3. جرب حذف `.htaccess`:
إذا استمرت المشكلة، احذف `htaccess.` مؤقتاً.

### 4. تحقق من الأذونات:
```
chmod 755 server
chmod 644 server/index.mjs
chmod -R 755 public
```

### 5. أعد تشغيل Node.js App:
- اضغط **"Restart"** في لوحة التحكم

---

## 📝 ملاحظة مهمة:

إذا كانت الملفات في `public_html` مباشرة (public, server)، فأنت **لا تحتاج** إلى مجلد `dist`. 

البنية الصحيحة:
- `public/` → الملفات الثابتة
- `server/` → كود الخادم
- Start Command: `node server/index.mjs`

